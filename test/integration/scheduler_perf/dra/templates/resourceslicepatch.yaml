apiVersion: resource.k8s.io/v1alpha3
kind: ResourceSlicePatch
metadata:
  generateName: test-patch-
spec:
  devices:
    priority: {{ .Index }}
    filter:
      deviceClass: test-class
      pool: scheduler-perf-dra-{{ .Index }}
    attributes:
      dra.example.com/patched:
        bool: true
